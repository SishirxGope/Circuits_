# [AI-GEN] agent=Claude date=2026-09-14 task=Real task prompt sets for the dense-node pipeline (Stage A engineering)
# reviewed-by: PENDING

"""Real (non-synthetic) task prompt sets: clean/corrupted pairs plus a task metric.

Engineering zone. Requires the ``models`` extra (torch + a tokenizer); nothing in the
engineering dry-run imports this package. Synthetic prompts for dry-runs remain in
``src/synthetic/synthetic_tasks.py``.
"""
