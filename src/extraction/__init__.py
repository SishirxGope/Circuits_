# [AI-GEN] agent=OpenCode date=2026-08-07 task=Create package markers
# reviewed-by: PENDING

"""Circuit extraction (proposal §2.2): attribution_graph.py, edge_pruning_graph.py,
dense_node_variant.py. Engineering zone (AI_RULES.md §3): wrappers around public
code (circuit-tracer; edge-pruning pipeline B). Every extraction threshold must
remain an ensemble axis, never hardcoded (CLAUDE.md §5).
"""
